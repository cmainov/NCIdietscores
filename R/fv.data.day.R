#' Test Data for `fvs_scores_day`
#'
#' Test data for examples in the `fvs_scores_day` function.
#'
#' @format `fv.data.day`
#' \describe{
#' A data frame with 45 rows and 20 columns:
#'   \item{Q1}{Fruit juice frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q1A}{Fruit juice portion size. A value in (0-3, M, E).}
#'   \item{Q2}{Fruit. A value in (0-9, M, E).}
#'   \item{Q2A1}{Fruit portion size option A. A value in (0-3, M, E).}
#'   \item{Q2A2}{Fruit portion size option B. A value in (0-3, M, E).}
#'   \item{Q3}{Salad or lettuce frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q3A}{Salad or lettuce portion size. A value in (0-3, M, E).}
#'   \item{Q4}{French fry frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q4A}{French fry portion size. A value in (0-3, M, E).}
#'   \item{Q5}{Other potatoes frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q5A}{Other potatoes portion size. A value in (0-3, M, E).}
#'   \item{Q6}{Beansfrequency of consumption. A value in (0-9, M, E).}
#'   \item{Q6A}{Beans portion size. A value in (0-3, M, E).}
#'   \item{Q7}{Other vegetables frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q7A}{Other vegetables portion size. A value in (0-3, M, E).}
#'   \item{Q8}{Tomato sauce frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q8A}{Tomato sauce portion size. A value in (0-3, M, E).}
#'   \item{Q9}{Vegetable souop frequency of consumption. A value in (0-9, M, E).}
#'   \item{Q9A}{Vegetable soup portion size. A value in (0-3, M, E).}
#'   \item{Q10}{Mixtures with vegetables frequency of consumption. A value in (0-9, M, E).}
#' }
#' @source <https://epi.grants.cancer.gov/diet/screeners/fruitveg/scoring/allday.html>
"fv.data.day"
